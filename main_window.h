#ifndef MAIN_WINDOW_H
    #define MAIN_WINDOW_H MAIN_WINDOW_H

    void end(GtkWidget *widget, gpointer data);
    GtkWidget *create_window(const char *headline, const int width, const int height);

#endif
